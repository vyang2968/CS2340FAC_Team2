<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SprintProject"
        tools:targetApi="31">
        <service
            android:name=".repository.MyService"
            android:enabled="true"
            android:exported="true"></service>

        <activity
            android:name=".view.SplashScreen"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".view.WelcomeScreen" />
        <activity android:name=".view.LoginScreen" />
        <activity android:name=".view.RegisterScreen" />
        <activity android:name=".view.AccommodationScreen" />
        <activity android:name=".view.DestinationScreen" />
        <activity android:name=".view.DiningEstablishmentScreen" />
        <activity android:name=".view.LogisticsScreen" />
        <activity android:name=".view.TransportationScreen" />
        <activity android:name=".view.TravelCommunity" />
    </application>

</manifest>